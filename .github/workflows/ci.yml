name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,mcp]"

      - name: Lint with ruff
        run: python -m ruff check .

      - name: Test with pytest
        run: python -m pytest tests/ -q --tb=short

      - name: Smoke test (imports)
        run: |
          python -c "from emsal_mcp.server import main"
          python -m emsal_mcp.cli version

      - name: Release readiness
        run: python -m emsal_mcp.cli release v1-readiness --json

  type-check:
    runs-on: ubuntu-latest
    continue-on-error: true  # informational, not blocking

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,mcp]"

      - name: Type check with mypy
        run: python -m mypy src/emsal_mcp/ --ignore-missing-imports
